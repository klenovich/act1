import React, { PropsWithChildren } from 'react';
import type { UnknownType } from '../../types/types';
declare type ActiveChannels = React.MutableRefObject<string[]>;
export declare const ActiveChannelsProvider: ({ children, value, }: React.PropsWithChildren<{
    value: ActiveChannels;
}>) => JSX.Element;
export declare const useActiveChannelsRefContext: () => ActiveChannels;
export declare const withActiveChannelsRefContext: <P extends UnknownType>(Component: React.ComponentType<P>) => React.FC<Omit<P, "activeChannels">>;
export {};
